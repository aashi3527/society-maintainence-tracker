# Secret for signing JWTs
JWT_SECRET=your_jwt_secret_key_here

# Code required to sign up as an Admin
ADMIN_SETUP_CODE=admin_pass_2026

# SMTP Configuration (Optional: if not provided, email notifications fall back to local database logging)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
